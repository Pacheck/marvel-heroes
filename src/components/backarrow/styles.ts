import styled from 'styled-components';

export const Navigation = styled.nav`
  height: 5rem;
  display: flex;
  align-items: flex-end;
`;
export const BackToHome = styled.img`
  margin-left: 7rem;
  opacity: 0.9;
  :hover {
    opacity: 1;
    cursor: pointer;
    transform: scale(1.1, 1.1);
  }
`;
